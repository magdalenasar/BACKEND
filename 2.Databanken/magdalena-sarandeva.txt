--1SELECT det_txt_nl as Gemeente, SUM(det_cases) AS 'Aantal besmettingen'FROM gemeenteWHERE det_txt_nl LIKE 'C%'GROUP BY det_txt_nlORDER BY 'Aantal besmettingen' DESC;--2select prv_naam, count(prv_id) as 'Aantal gemeenten'from gemeente gleft join provincie p on g.det_prv_id = p.prv_idORDER BY 'Aantal gemeenten';--3select reg_name, sum(det_cases) as 'Totaal besmettingen'from regioinner join gemeente g on g.det_id = det_prv_idgroup by reg_nameorder by reg_name;--4select reg_name, round(sum(det_cases) as 'Totaal besmettingen'), count(*)from regioinner join gemeente g on g.det_id = det_prv_idgroup by reg_nameorder by reg_name;--5select prv_naam, prv_naam, avg(det_cases) as 'Gemiddeld aantal besmettingen per gemeente'from provincie pinner join regio r on p.prv_reg_id = r.reg_idorder by provincie asc;-- 6.create table centra(   cen_id int auto_increment,   cen_naam varchar(255) null,   cen_det_id int null,   constraint centra_pk      primary key (cen_id));